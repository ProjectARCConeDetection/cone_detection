<?xml version="1.0" encoding="utf-8"?>	
	
<launch>

  <rosparam command="load" file="$(find cone_detection)/cfg/parameter.yaml" />

  <!-- CONVOLUTIONAL NETWORK -->
  <node name="train" pkg="cone_detection" type="train.py" output="screen" required="true" />

</launch>

